<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".QuestionnaireEnd"></activity>
        <activity android:name=".QuestionnaireStart" />
        <activity android:name=".StressReliefActivities" />
        <activity android:name=".WebView" />
        <activity android:name=".Custom_Progressbar" />
        <activity android:name=".circular_status_bar" />
        <activity android:name=".PresentStressActivity" />
        <activity android:name=".PerceivedStressActivity" />
        <activity android:name=".ChooseStressTypeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>